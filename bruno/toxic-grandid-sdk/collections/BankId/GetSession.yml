info:
  name: GetSession
  type: http
  seq: 2

http:
  method: POST
  url: "{{url}}/GetSession"
  body:
    type: multipart-form
    data:
      - name: apiKey
        type: text
        value: "{{api-key}}"
      - name: authenticateServiceKey
        type: text
        value: "{{service-key}}"
      - name: sessionId
        type: text
        value: 5c66343fad1321579408d57f19d658d9

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
